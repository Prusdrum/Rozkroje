const meta = require('../package.json');

const appConfig = {
    CHANGELOG_URL: 'https://github.com/Prusdrum/Rozkroje/blob/master/CHANGELOG.md',
    VERSION: meta.version
};

module.exports = appConfig;